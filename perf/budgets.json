{
  "version": 1,
  "description": "Perf budgets for Memex — enforced in CI via size-limit",
  "budgets": [
    {
      "path": "apps/desktop/dist",
      "maxSizeKB": 500,
      "comment": "Tauri frontend bundle (vite) must stay <500kB (gzip ~120kB, see size-limit)"
    },
    {
      "path": "apps/desktop/dist/assets",
      "maxSizeKB": 400,
      "comment": "JS/CSS assets budget (JS 500kB gzip, CSS 100kB gzip via size-limit)"
    }
  ],
  "ci": {
    "enforced": true,
    "tool": "size-limit",
    "onExceed": "fail"
  }
}
