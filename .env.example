# Root env example — see apps/desktop/.env.example for Tauri variables
# Copy to .env if you need local overrides: `cp .env.example .env`
TAURI_DEV_HOST=
# Optional: override Vite port (Tauri expects 1420)
# VITE_PORT=1420
